#!/bin/bash

# Update and upgrade the system
sudo apt-get update
sudo apt-get upgrade -y

# Install required software
sudo apt-get install -y python3 python3-pip mosquitto mosquitto-clients build-essential libhdf5-dev

# Create and activate a virtual environment
python3 -m venv /home/crest/venv
source /home/crest/venv/bin/activate

# Upgrade pip, setuptools, and wheel
pip install --upgrade pip setuptools wheel

# Install required Python libraries
pip install tensorflow paho-mqtt numpy flask transformers scikit-learn kaggle

# Transfer the code
scp -r /path/to/your/code crest@10.88.0.101:/home/crest/edge


Matt config file (Mac): /opt/homebrew/etc/mosquitto/mosquitto.conf